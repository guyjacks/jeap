template_parent_type: default to object or array
- tells the compiler what to assume otherwise users would have to specify brackets or braces in each template.
- brackets or braces override this setting
